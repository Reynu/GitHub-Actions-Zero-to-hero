name: Docker Build and push

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: login to docker hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHB_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}
              
      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: 
          push: true
          tags: |
            ${{ vars.DOCKERHB_USERNAME }}/github-actions-app:latest
            ${{ vars.DOCKERHB_USERNAME }}/github-actions-app:${{ github.sha }}
            ${{ vars.DOCKERHB_USERNAME }}/github-actions-app:${{ github.ref_name }}